# 霓虹坦克竞技场（Neon Tank Arena）— 技术开发文档

> 面向 Solo Builder 的可执行规格说明书
> 版本：v2.0（4 人混战 + 子弹反弹玩法）
> 关键词：科技风、Canvas、4 坦克对战、子弹弹射、3 AI + 1 玩家、单机

---

## 一、项目概述

### 1.1 项目定位
一款**纯前端、单文件部署**的网页 4 坦克对战游戏。封闭竞技场内 4 辆坦克混战，子弹撞墙反弹，被击中即死，最后存活者胜。玩家操控 1 辆，其余 3 辆由 AI 控制。

### 1.2 核心体验目标
| 维度 | 目标 |
|------|------|
| 核心机制 | 子弹反弹（弹射）+ 1 击毙命 + 4 方混战 |
| 视觉 | 赛博朋克霓虹竞技场，深色背景 + 青/紫/红/绿四色坦克识别 |
| 节奏 | 单局 30 秒 - 2 分钟，多局累计积分定胜负 |
| 上手 | 5 秒看懂，移动+射击两键操作 |
| 深度 | AI 智能走位、反弹预判、武器道具策略 |

### 1.3 技术约束
- **纯前端**：HTML + CSS + JavaScript，不依赖后端
- **零构建**：可直接双击 `index.html` 运行，无需 npm/webpack
- **单文件优先**：理想状态一个 HTML 文件包含全部逻辑（便于分发）
- **零外部依赖**：所有素材用 Canvas 程序化绘制 + Web Audio 合成音效
- **逻辑分辨率**：900×600 竞技场（宽屏，适合横屏对战）

### 1.4 目标平台
- 桌面浏览器（Chrome/Edge/Firefox/Safari 最新版）
- 移动端浏览器（支持触屏虚拟摇杆 + 射击键）

---

## 二、技术选型

### 2.1 渲染方案
**HTML5 Canvas 2D**（不使用 WebGL / 游戏引擎）

| 选项 | 选择 | 理由 |
|------|------|------|
| 游戏引擎 | ❌ 不用 Phaser 等 | Solo 维护成本高，单文件难打包 |
| 渲染层 | ✅ 原生 Canvas 2D | 足够实现霓虹风，零依赖 |
| 物理引擎 | ❌ 不用 Matter.js | 反弹用几何反射定律手写更轻、更可控 |
| 音效 | ✅ Web Audio API 合成 | 免素材、免加载 |

### 2.2 代码组织
```
index.html
├── <style>      // 全部样式
├── <canvas>     // 游戏画布
└── <script>     // 全部逻辑（模块化 Class）
```

脚本内部模块（均在同一 `<script>` 内）：
- `Config` 配置常量
- `Input` 输入层（键盘 + 触屏）
- `Math2D` 几何工具（向量、反射、线段相交）
- `Entity` 实体基类（Tank / Bullet / PowerUp / Effect / Mine）
- `Map` 地图与墙体系统
- `Collision` 碰撞与反弹系统
- `AI` 敌人 AI（寻路 + 躲避 + 射击决策）
- `Particle` 粒子系统
- `Audio` 音效合成
- `Scene` 场景管理（菜单/对战/暂停/结算）
- `Game` 主循环与状态机
- `Storage` 本地存档（最高分、设置）

---

## 三、核心玩法设计

### 3.1 核心规则
1. **4 坦克混战**：玩家 1 + AI 3，颜色区分（青/紫/红/绿）
2. **1 击毙命**：被任何子弹击中立即死亡（无血量概念，护盾道具除外）
3. **子弹反弹**：子弹撞墙按反射定律弹射，最多反弹 N 次后消失
4. **自伤机制**：子弹反弹后可击中发射者本人（增加策略深度）
5. **同屏子弹限制**：每辆坦克同屏最多 1-3 发（随武器变化）
6. **死亡观战**：死亡后可继续观战，或等待下一局

### 3.2 回合与积分
| 项 | 规则 |
|----|------|
| 单局结束条件 | 仅剩 1 辆存活，或所有人死亡（平局） |
| 击杀得分 | +2 分 |
| 存活到最后 | +1 分 |
| 目标分数 | 先达到 10 分者获胜（可设置 5/10/15/20） |
| 局间间隔 | 3 秒过渡，自动开始下一局 |

### 3.3 武器系统（多样性核心）

| 武器 | 子弹数 | 反弹次数 | 速度 | 特性 | 获取 |
|------|--------|----------|------|------|------|
| 普通弹 | 1 | 3 | 中 | 默认装备 | 初始 |
| 霰弹 | 3 扇形 | 2 | 中 | 三发扇形散射 | 道具 |
| 穿甲弹 | 1 | 1 | 快 | 高速、低反弹 | 道具 |
| 追踪弹 | 1 | 2 | 慢 | 缓慢追踪最近敌人 | 道具 |
| 激光 | 1 | 0 | 即时 | 即时射线、不反弹、穿透 | 道具 |
| 等离子球 | 1 | 5 | 慢 | 高反弹、体积大 | 道具 |
| 导弹 | 1 | 0 | 中 | 命中爆炸范围伤害 | 道具 |

武器为**临时强化**，拾取后保持 N 秒或 N 发后恢复普通弹。

### 3.4 道具系统（地图随机刷新）

| 道具 | 效果 | 颜色 | 刷新概率 |
|------|------|------|----------|
| 武器箱 | 随机获得上述武器 | 🎁 金色 | 高 |
| 护盾 | 抵挡 1 次伤害 | 🛡 青色 | 中 |
| 加速 | 移速 +60% 持续 5 秒 | ⚡ 蓝色 | 中 |
| 多发 | 同屏子弹 +2 持续 8 秒 | 💥 紫色 | 中 |
| 隐身 | 短暂半透明 4 秒 | � 灰色 | 低 |
| 地雷 | 放置 1 颗地雷，触发爆炸 | 💣 红色 | 低 |
| 视野干扰 | 使其他坦克视野模糊 3 秒 | 🌀 粉色 | 低 |

道具每隔 5-8 秒在地图空位随机刷新 1 个，同屏最多 3 个。

### 3.5 游戏模式

| 模式 | 说明 | 解锁 |
|------|------|------|
| 混战（FFA） | 4 人各自为战，标准玩法 | 默认 |
| 2v2 组队 | 玩家 + 1 AI vs 2 AI | 默认 |
| 限时模式 | 5 分钟内得分最高者胜 | 默认 |
| 极速模式 | 子弹速度 +50%，无反弹上限 | 通关混战 1 局 |
| 道具狂欢 | 道具刷新频率 ×3 | 通关混战 3 局 |
| 噩梦模式 | AI 全部最高难度 | 累计 10 胜 |

---

## 四、游戏系统设计

### 4.1 坦克属性结构
```js
{
  id: 0,                  // 0=玩家, 1-3=AI
  name: 'Player',
  color: '#00f0ff',       // 霓虹色
  x, y,                   // 位置（中心点）
  angle,                  // 朝向（弧度）
  speed: 2.2,             // 移动速度
  rotSpeed: 0.06,         // 转向速度
  alive: true,
  weapon: 'normal',       // 当前武器
  weaponAmmo: -1,         // 剩余弹药，-1=无限
  weaponTimer: 0,         // 剩余时间（帧）
  shield: 0,              // 护盾次数
  speedBoost: 0,          // 加速剩余帧
  multiShot: 0,           // 多发剩余帧
  stealth: 0,             // 隐身剩余帧
  score: 0,               // 累计得分
  kills: 0,               // 击杀数
  fireCooldown: 0,        // 射击冷却
  lives: 1,               // 剩余命（每局重置）
}
```

### 4.2 子弹结构
```js
{
  ownerId: 0,             // 发射者
  x, y,                   // 位置
  vx, vy,                 // 速度向量
  speed: 6,               // 速度大小
  bouncesLeft: 3,         // 剩余反弹次数
  maxBounces: 3,
  weapon: 'normal',       // 武器类型
  radius: 4,              // 碰撞半径
  life: 300,              // 最大存活帧
  trail: [],              // 拖尾轨迹点
  homing: false,          // 是否追踪
  explosive: false,       // 是否爆炸
}
```

### 4.3 地图系统

#### 4.3.1 地图元素
| 元素 | 可通过 | 子弹反弹 | 说明 |
|------|--------|----------|------|
| 空地 | ✅ | — | 自由移动 |
| 实心墙 | ❌ | ✅ | 不可破坏，子弹反弹 |
| 反弹板 | ❌ | ✅×2 | 特殊墙，反弹速度+20%（可选） |
| 可破坏墙 | ❌ | ✅ | 受 3 次命中后破碎（可选玩法） |
| 出生点候选区 | ✅ | — | 每地图预设 8-12 个点位，每局随机抽 4 个 |

#### 4.3.2 地图布局（多张预设）
内置 6-8 张地图，每局可选/随机/轮换：
1. **十字迷宫**：中心十字墙体，多点出生
2. **回字阵**：同心矩形墙，中间战场
3. **柱阵**：散布圆柱形障碍
4. **走廊**：横向走廊 + 侧室
5. **斜角阵**：斜向墙体，反弹角度刁钻
6. **空旷场**：仅四周墙，纯走位对战
7. **迷宫**：复杂通道
8. **弹球迷宫**：多个反弹板

#### 4.3.3 地图数据结构
```js
{
  id: 1,
  name: '十字迷宫',
  width: 900, height: 600,
  walls: [
    { x: 100, y: 100, w: 700, h: 20 },   // 矩形墙
    { x: 440, y: 200, w: 20, h: 200 },
    // ... 可为圆墙：{ type:'circle', cx, cy, r }
  ],
  spawns: [ /* 8-12 个候选出生点，每局随机抽 4 个分配给 4 辆坦克 */ ],
  decor: [],   // 纯装饰元素
}
```

### 4.4 AI 系统

#### 4.4.1 AI 难度分级
| 等级 | 移动 | 射击 | 躲避 | 寻路 |
|------|------|------|------|------|
| 菜鸟 | 随机游走 | 朝向大致方向 | 不躲避 | 随机 |
| 普通 | 偶尔追道具 | 朝向精准 | 30% 概率躲避 | BFS 简单寻路 |
| 困难 | 主动抢道具 | 预判射击 | 70% 概率躲避 | A* 寻路 |
| 噩梦 | 战术走位 | 反弹预判射击 | 95% 概率躲避 | A* + 路径评估 |

#### 4.4.2 AI 行为状态机
```
WANDER（漫游）
   ↓ 发现敌人/道具
CHASE（追击道具或敌人）
   ↓ 进入射程且瞄准
AIM（瞄准）
   ↓ 瞄准完成
FIRE（射击）
   ↓ 子弹来袭
DODGE（躲避）← 紧急状态，最高优先级
```

#### 4.4.3 AI 反弹预判（高难度）
模拟未来 N 帧子弹轨迹（含反弹），找到能命中敌人的射击角度。计算成本高，仅噩梦难度启用，且每 30 帧计算一次。

#### 4.4.4 AI 躲避子弹
- 检测所有来袭子弹的轨迹（含反弹后路径）
- 计算最近交点距离 + 时间
- 若 < 安全阈值，向垂直方向移动

### 4.5 存档系统
```js
localStorage['neonTankArena'] = {
  highScore: 0,
  totalGames: 0,
  totalKills: 0,
  totalWins: 0,
  unlockedModes: ['ffa', 'team', 'timed'],
  settings: { sound: true, music: true, aiDifficulty: 'normal', targetScore: 10 }
}
```

---

## 五、系统架构

### 5.1 游戏主循环
```js
let lastTime = 0;
function gameLoop(timestamp) {
  const dt = Math.min(timestamp - lastTime, 33);  // 限制最大帧间隔
  lastTime = timestamp;
  update(dt);
  render();
  requestAnimationFrame(gameLoop);
}
```

### 5.2 状态机
```
BOOT → MENU → MODE_SELECT → MAP_SELECT → PLAYING
                                              ↓
                                        PAUSED ↔ PLAYING
                                              ↓
                                        ROUND_END → PLAYING (下一局)
                                              ↓
                                        MATCH_END → MENU
```

### 5.3 实体管理
- 统一数组：`tanks` / `bullets` / `powerups` / `particles` / `mines`
- 每帧顺序：`input → AI → update → collision → render`
- 死亡标记 `dead = true`，循环末清理

### 5.4 事件系统（发布订阅）
```js
EventBus.on('tankKilled', (victim, killer) => { ... });
EventBus.emit('tankKilled', victim, killer);
```
用于解耦：击杀→计分、播音效、生成粒子、检测回合结束。

---

## 六、关键技术实现

### 6.1 子弹反弹（核心机制）

#### 6.1.1 反射定律
子弹撞水平墙：`vy = -vy`
子弹撞垂直墙：`vx = -vx`
子弹撞圆墙：沿法线反射

#### 6.1.2 碰撞检测算法
子弹作为移动线段（前一帧位置 → 当前位置），与墙做**线段-矩形**相交检测，取最早交点，在该点反射。

```js
function updateBullet(b, dt) {
  const oldX = b.x, oldY = b.y;
  b.x += b.vx * dt;
  b.y += b.vy * dt;

  // 检测与所有墙的碰撞
  let hit = checkBulletWalls(oldX, oldY, b.x, b.y);
  if (hit) {
    if (hit.axis === 'h') b.vy = -b.vy;     // 水平墙，Y 速度反转
    if (hit.axis === 'v') b.vx = -b.vx;     // 垂直墙，X 速度反转
    b.x = hit.x; b.y = hit.y;
    b.bouncesLeft--;
    spawnSparkParticles(hit.x, hit.y);
    playSound('bounce');
  }
  if (b.bouncesLeft < 0 || b.life-- < 0) b.dead = true;
}
```

#### 6.1.3 多墙连续反弹处理
单帧内若速度极快可能穿越多面墙，需循环检测直到该帧无碰撞或反弹次数耗尽（最多迭代 3 次防死循环）。

### 6.2 坦克移动与转向

#### 6.2.1 移动模型
坦克有独立朝向 `angle` 与移动方向：
- 转向：左/右键调整 `angle`
- 前进/后退：沿 `angle` 方向移动
- 类似经典坦克操控（参考 FC Battle City）

#### 6.2.2 墙体阻挡
坦克 AABB 与墙矩形碰撞，按轴向分离推进（先 X 后 Y），避免穿墙。

### 6.3 碰撞检测

| 检测对 | 方法 |
|--------|------|
| 坦克 vs 墙 | AABB-矩形，轴向分离 |
| 子弹 vs 墙 | 线段-矩形相交 |
| 子弹 vs 坦克 | 圆-矩形（子弹为圆，坦克为矩形） |
| 子弹 vs 子弹 | 圆-圆（可选，同发相消） |
| 坦克 vs 道具 | AABB |
| 坦克 vs 地雷 | AABB |

### 6.4 AI 寻路（BFS / A*）

#### 6.4.1 网格化
将 900×600 地图划分为 30×30 网格（30×20 = 600 格），墙占用对应格子。

#### 6.4.2 BFS 简单寻路（普通难度）
```js
function bfsPath(start, goal) {
  const queue = [start];
  const visited = new Set([key(start)]);
  const parent = {};
  while (queue.length) {
    const cur = queue.shift();
    if (eq(cur, goal)) return reconstruct(parent, cur);
    for (const next of neighbors(cur)) {
      if (!visited.has(key(next)) && !isWall(next)) {
        visited.add(key(next));
        parent[key(next)] = cur;
        queue.push(next);
      }
    }
  }
  return null;
}
```

#### 6.4.3 路径平滑
BFS 返回的格点路径需平滑为更自然的曲线，按可视性测试合并直线段。

### 6.5 渲染系统

#### 6.5.1 双缓冲
所有绘制先到离屏 Canvas，最后一次 `drawImage` 到主 Canvas，避免闪烁。

#### 6.5.2 霓虹特效
```js
ctx.shadowBlur = 15;
ctx.shadowColor = tank.color;
ctx.strokeStyle = tank.color;
ctx.lineWidth = 2;
ctx.strokeRect(...);
ctx.shadowBlur = 0;   // 关键：用完关闭，避免性能损耗
```

#### 6.5.3 程序化坦克绘制
```js
function drawTank(ctx, t) {
  ctx.save();
  ctx.translate(t.x, t.y);
  ctx.rotate(t.angle);
  // 履带、车身、炮塔、炮管分层绘制
  // 颜色用 t.color，加 shadowBlur 外发光
  ctx.restore();
}
```

#### 6.5.4 子弹拖尾
每帧记录子弹位置到 `trail` 数组（保留最近 8 帧），按透明度渐变绘制线段。

#### 6.5.5 背景层
- 深色渐变背景（#0a0a1a → #1a0a2a）
- 动态网格线（缓慢流动）
- 扫描线效果（半透明水平条纹下移）
- 远景星点

### 6.6 粒子系统

| 类型 | 触发 | 视觉 |
|------|------|------|
| 爆炸 | 坦克死亡 | 放射状火花 + 烟雾环 |
| 火花 | 子弹反弹 | 小型四溅 |
| 拖尾 | 子弹飞行 | 渐隐光带 |
| 拾取 | 拾取道具 | 上升光柱 |
| 环境 | 持续 | 背景漂浮粒子 |

粒子结构：`{ x, y, vx, vy, life, maxLife, color, size, fade }`，对象池复用避免 GC。

### 6.7 音效系统（Web Audio 合成）
```js
const AC = new (window.AudioContext || window.webkitAudioContext)();
function playSound(type) {
  const osc = AC.createOscillator();
  const gain = AC.createGain();
  osc.connect(gain); gain.connect(AC.destination);
  const t = AC.currentTime;
  switch (type) {
    case 'shoot':    osc.type='square'; osc.frequency.setValueAtTime(800,t); osc.frequency.exponentialRampToValueAtTime(200,t+0.1); break;
    case 'bounce':   osc.type='triangle'; osc.frequency.setValueAtTime(1200,t); osc.frequency.exponentialRampToValueAtTime(600,t+0.05); break;
    case 'explode':  /* 白噪声 + 低通滤波 */ break;
    case 'powerup':  /* 上升音阶 */ break;
    case 'death':    /* 下降音阶 */ break;
  }
  gain.gain.setValueAtTime(0.2, t);
  gain.gain.exponentialRampToValueAtTime(0.01, t+0.3);
  osc.start(t); osc.stop(t+0.3);
}
```
音效清单：shoot / bounce / explode / powerup / death / menuMove / menuSelect / roundWin / matchWin

### 6.8 输入系统

#### 6.8.1 桌面端
- 方向键 / WASD：转向 + 前后
- 空格 / J：射击
- Esc / P：暂停
- Enter：确认

#### 6.8.2 移动端
- 左下虚拟摇杆（8 方向）
- 右下射击按钮
- 顶部暂停按钮
- 自动检测触屏：`'ontouchstart' in window`

---

## 七、UI/UX 设计

### 7.1 视觉风格定义

#### 7.1.1 配色方案
```
背景深色    #0a0a1a
玩家青      #00f0ff
AI 紫       #b400ff
AI 红       #ff0040
AI 绿       #00ff88
墙体霓虹    #4a4a8a（带白边发光）
道具金      #ffea00
文字白      #e0e0ff
```

#### 7.1.2 字体
- 标题：等宽科技字体（'Orbitron' / 系统等宽回退）
- UI：'Consolas' / 'Courier New' / monospace
- 不引入外部字体文件

### 7.2 界面流程
```
启动画面（Logo + 加载动画）
   ↓
主菜单（开始游戏 / 模式选择 / 设置 / 帮助 / 排行）
   ↓
模式选择 → AI 难度 / 目标分数设置 → 地图选择（指定/随机/轮换）
   ↓
战斗界面（4 坦克对战，出生点随机分配）
   ↓
暂停界面（继续 / 重开 / 退出）
   ↓
回合结算（本局胜者 + 当前积分）
   ↓
达成目标分数 → 比赛结算（总冠军 + 统计）
```

### 7.3 HUD 设计
```
┌─────────────────────────────────────────────────────┐
│  局 3   目标 10 分          ⏸ 暂停                  │
├──────────────┬───────────────┬──────────────────────┤
│ ●玩家  6 分  │ ●紫AI  4 分   │ ●红AI 3 分  ●绿AI 2分│
├──────────────┴───────────────┴──────────────────────┤
│                                                     │
│            竞技场画布 900×600                        │
│                                                     │
├─────────────────────────────────────────────────────┤
│  武器：霰弹 ×3   护盾：✗   加速：✗                  │
└─────────────────────────────────────────────────────┘
```

死亡后坦克显示为半透明观战状态，HUD 标记为灰色。

### 7.4 动效细节
- 按钮 hover：霓虹边框扩散 + 轻微缩放
- 菜单切换：扫描线滑入 + 淡入
- 击杀提示：浮动"+2"飘起淡出
- 回合切换：故障字效（glitch）+ 扫描
- 受击/死亡：屏幕对应颜色 vignette 闪烁
- 胜利：冠军坦克环绕粒子 + 烟花

---

## 八、地图设计

### 8.1 内置地图清单（建议 8 张）
| ID | 名称 | 特点 | 适合模式 |
|----|------|------|----------|
| 1 | 十字迷宫 | 中心十字墙，四角出生 | 全模式 |
| 2 | 回字阵 | 同心矩形墙，中央战场 | 混战 |
| 3 | 柱阵 | 散布圆柱障碍，反弹多变 | 混战 |
| 4 | 走廊战 | 横向走廊 + 侧室 | 2v2 |
| 5 | 斜角阵 | 斜向墙体，刁钻反弹 | 高手 |
| 6 | 空旷场 | 仅四周墙，纯走位 | 新手 |
| 7 | 迷宫 | 复杂通道 | 混战 |
| 8 | 弹板阵 | 多个反弹板，子弹狂飙 | 道具狂欢 |

### 8.2 地图选择
- **地图选择界面**：模式确定后进入地图选择页，展示所有地图缩略图与名称，支持三种选择方式：
  - 🎯 指定地图：玩家手动选定 1 张（适合练习特定地图）
  - 🎲 随机单图：系统随机抽 1 张（不重复上一张）
  - 🔄 循环轮换：每局自动切换下一张地图（默认选项）
- **出生点随机分配**：每张地图预设 8-12 个候选出生点，每局开局从中**随机抽取 4 个**分配给 4 辆坦克，且**坦克身份（玩家/3 个 AI）与出生点的对应关系也随机**，保证每局开局体验不同
- 抽取算法：从候选池无放回抽样 4 个，校验两两间距 ≥ 200 px，不满足则重抽（最多 10 次，失败则取最远点组合）

### 8.3 平衡性原则
- 候选出生点整体对称分布，任意 4 点组合无显著优劣势
- 出生点之间保持最小距离（≥ 200 px），避免开局瞬间交火
- 出生点附近 2 格（60 px）内无墙体遮挡，保证起步空间
- 出生点远离道具刷新点（≥ 100 px），避免开局即拾取
- 道具刷新点分布均匀
- 无死角位置（避免被堵死）

---

## 九、性能优化

### 9.1 渲染优化
- 离屏 Canvas 缓存静态层（地图、背景网格）
- `shadowBlur` 仅用于坦克/子弹/道具，墙体用静态预渲染
- 粒子数量上限（同屏 ≤ 200）
- 拖尾点数上限（每发 ≤ 8）

### 9.2 逻辑优化
- 子弹-墙碰撞：空间分区（网格哈希），避免遍历全部墙
- AI 寻路分帧计算（每帧算 1 个 AI，轮流）
- 实体对象池（子弹、粒子复用）
- 远离视野的实体不渲染

### 9.3 移动端适配
- 逻辑分辨率固定，CSS `image-rendering: pixelated` 缩放
- DPR 适配：`canvas.width = logicalSize * devicePixelRatio`
- 触屏事件 passive 监听
- 低性能模式：减少粒子、关闭扫描线、降低 AI 难度

### 9.4 性能指标
- 桌面端稳定 60 FPS
- 中端手机稳定 30-60 FPS
- 内存占用 < 80 MB
- 子弹同屏 ≤ 12 发无卡顿

---

## 十、开发计划与里程碑

### 10.1 分阶段交付（建议 Solo 顺序）
| 阶段 | 内容 | 预估工时 |
|------|------|----------|
| M1 骨架 | HTML 结构 + Canvas 初始化 + 主循环 + 输入 | 0.5 天 |
| M2 地图 | 墙体渲染 + 坦克移动 + 墙体阻挡 | 1 天 |
| M3 射击 | 子弹发射 + 反弹机制 + 子弹-墙碰撞 | 1 天 |
| M4 击杀 | 子弹-坦克碰撞 + 死亡 + 重生 | 0.5 天 |
| M5 AI | 基础 AI（漫游+射击）+ BFS 寻路 | 1.5 天 |
| M6 系统 | 回合制 + 积分 + 4 坦克配置 | 1 天 |
| M7 武器道具 | 7 种武器 + 7 种道具 + 刷新 | 1.5 天 |
| M8 视觉 | 霓虹风 + 粒子 + 背景 + 动效 | 1.5 天 |
| M9 音效 | Web Audio 合成全套音效 | 0.5 天 |
| M10 内容 | 8 张地图 + 地图选择界面 + 多模式 + AI 难度 | 1.5 天 |
| M11 体验 | 菜单/结算/存档/移动端/平衡 | 1 天 |
| M12 优化 | 性能调优 + Bug 修复 + 打磨 | 1 天 |
| **合计** | | **约 12.5 天** |

### 10.2 MVP 定义（最小可玩版本）
M1-M6 完成 = 4 坦克可对战，子弹反弹，有积分，1 张地图，普通 AI。作为第一个 demo 验收点。

---

## 十一、交付清单

### 11.1 代码交付
- [ ] `index.html` 单文件（含全部 CSS/JS）
- [ ] 可直接双击运行
- [ ] 移动端可触屏游玩
- [ ] 控制台无报错

### 11.2 内容交付
- [ ] 8 张地图
- [ ] 7 种武器
- [ ] 7 种道具
- [ ] 4 档 AI 难度
- [ ] 6 种游戏模式
- [ ] 完整音效集
- [ ] 本地存档（设置 + 统计）

### 11.3 质量要求
- [ ] 桌面端 60 FPS
- [ ] 子弹反弹几何正确（无穿墙、无卡墙）
- [ ] 自伤机制正确（反弹后可击中自己）
- [ ] 回合/积分/胜负判定无误
- [ ] AI 不卡死、不穿墙
- [ ] 暂停/恢复状态无损
- [ ] 键盘 + 触屏双输入正常

---

## 十二、附录

### 12.1 参考玩法
- 《Tank Trouble》（浏览器经典 2 人对战坦克，子弹反弹核心机制）
- 《Atomic Tanks》（回合制坦克对战）
- 《Wii Play Tanks》（俯视角坦克，敌人 AI 优秀）
- 赛博朋克 2077 视觉风格参考

### 12.2 关键参数速查表
| 参数 | 值 | 备注 |
|------|-----|------|
| 逻辑分辨率 | 900×600 | 横屏竞技场 |
| 帧率目标 | 60 FPS | requestAnimationFrame |
| 坦克尺寸 | 28×28 px | 略小于格子 |
| 坦克移速 | 2.2 px/帧 | 加速时 3.5 |
| 转向速度 | 0.06 弧度/帧 | 约 3.4°/帧 |
| 子弹速度 | 6 px/帧 | 穿甲弹 9 |
| 子弹半径 | 4 px | 等离子球 8 |
| 普通弹反弹 | 3 次 | 等离子球 5 次 |
| 子弹存活 | 300 帧（5 秒） | 防止永久弹射 |
| 射击冷却 | 25 帧（~0.4 秒） | |
| 道具刷新间隔 | 300-480 帧（5-8 秒） | |
| 同屏道具上限 | 3 | |
| 目标分数（默认） | 10 分 | 可选 5/10/15/20 |
| 网格大小 | 30×30 px | 用于 AI 寻路 |
| 候选出生点数 | 8-12 个/地图 | 每局随机抽 4 个 |
| 出生点最小间距 | ≥ 200 px | 避免开局即交火 |
| 出生点墙体清空区 | 60 px 半径 | 保证起步空间 |

### 12.3 反弹几何核心公式
```
水平墙（上下边）：vy → -vy
垂直墙（左右边）：vx → -vx
矩形墙四边：判断击中哪条边，对应轴反转
圆墙：法线 = 击点 - 圆心
  v' = v - 2(v·n)n
```

### 12.4 待你补充/决策的事项
> 以下为开放问题，建议你在文档基础上明确后再交 Builder：

1. **操控方式**：转向+前后（FC 风）vs 8 方向直接移动（更现代易上手）？
2. **自伤**：反弹子弹能否击中自己？（建议：能，增加策略性）
3. **队友伤害**：2v2 模式下队友子弹是否伤害自己人？
4. **武器持续时间**：按时间（8 秒）vs 按弹药（5 发）？
5. **地图可破坏墙**：是否引入？会增加复杂度
6. **AI 数量**：固定 3 AI vs 可调（2-3 AI）？
7. **音效风格**：8-bit 复古 vs 合成器科幻？
8. **难度选项**：是否提供 简单/普通/困难 三档全局难度？
9. **分享功能**：是否需要截图分享战绩？
10. **多语言**：仅中文 vs 中英双语？

---

**文档结束。请在本文档基础上修订后交由 Builder 执行。**
